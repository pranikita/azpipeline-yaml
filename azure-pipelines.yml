# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool: build-agent-pool

steps:

- task: PowerShell@2
  displayName: Powershell scipt to echo
  inputs:
    filePath: 'script.ps1'





